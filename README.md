# Project Name

Short one-line project description.

## Getting started

1. Clone:
   ```
   git clone <repo-url>
   cd <repo-name>
   ```
2. Create a branch:
   ```
   git switch -c feature/your-feature
   ```
3. Install dependencies / run:
   - Example: `npm install` / `python -m pip install -r requirements.txt`

## Contributing

- Open an issue for major changes.
- Create a branch, push, and open a pull request.

## License

Choose a license and add it to the repo.
